# erupt-admin

All-in-one admin backend starter for Erupt.

Bundles `erupt-upms` (users & permissions) and `erupt-security` (API protection) into a single dependency. Add this module to your Spring Boot application to get a fully functional, secured admin backend with login, role management, and menu control — no extra configuration required.
