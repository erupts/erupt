# erupt-template-module

Starter template for building new Erupt extension modules.

Copy this module as a starting point when creating a custom Erupt plugin. Includes the standard `EruptModule` implementation skeleton, `AutoConfiguration` wiring, and `META-INF` service registration — everything needed to integrate cleanly into the Erupt module lifecycle.
