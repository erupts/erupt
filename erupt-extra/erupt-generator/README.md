# erupt-generator

Code generator for Erupt entities.

Introspects existing database tables and generates annotated Java entity classes (`@Erupt` + `@EruptField`) ready to drop into your project. Uses FreeMarker templates for customizable output. Eliminates boilerplate when migrating existing schemas to the Erupt framework.
