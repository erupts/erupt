# erupt-monitor

System monitoring dashboard for Erupt.

Displays real-time server metrics (CPU, memory, disk, network) via [oshi-core](https://github.com/oshi/oshi), JVM heap and thread stats, Redis info, and active Erupt sessions. All metrics are surfaced as standard Erupt admin UI pages — no custom frontend required.
