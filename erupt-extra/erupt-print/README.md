# erupt-print

Print and report rendering for Erupt.

Uses Apache Velocity templates to generate printable HTML output from `@Erupt` data models. Supports custom print layouts, variable substitution, and per-row print actions triggered directly from the admin UI.
