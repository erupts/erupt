# erupt-websocket

Authenticated WebSocket infrastructure for Erupt.

Provides a secure WebSocket layer integrated with `erupt-upms` token validation. Used internally by `erupt-notice` (push notifications) and `erupt-terminal` (browser shell). Can also be used directly to build real-time features in Erupt-based applications.
