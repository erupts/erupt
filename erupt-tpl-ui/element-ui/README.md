# erupt-tpl-ui · Element UI

Frontend static assets for Erupt based on **Element UI** (Vue 2, v2.15.0).

Packages the compiled Element UI admin UI as a Spring Boot static resource module. Include this dependency to serve the Element UI-flavored Erupt frontend from your application without any separate frontend build step.
