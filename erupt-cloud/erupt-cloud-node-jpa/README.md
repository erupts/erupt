# erupt-cloud-node-jpa

Pre-configured cloud node with JPA data layer.

Combines `erupt-cloud-node` and `erupt-jpa` into a single dependency for the common case of a relational-database-backed worker node. Also includes `erupt-toolkit` for caching support. Use this instead of `erupt-cloud-node` when your node application uses JPA.
