# erupt-cloud-common

Shared library for the Erupt distributed cloud architecture.

Contains common DTOs, constants, and utility classes used by both `erupt-cloud-server` (control plane) and `erupt-cloud-node` (worker nodes). Not intended for direct use in application projects.
