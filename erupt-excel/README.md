# erupt-excel

Excel import and export for Erupt data models.

Built on Apache POI. Integrates transparently with the data layer — annotated models gain Excel upload/download endpoints automatically. Supports template-based export and field-level mapping via `@EruptField` configuration.
