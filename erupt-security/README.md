# erupt-security

API security layer for Erupt.

Adds request interception, token validation, and data-level authorization on top of `erupt-upms`. Protects all Erupt-generated endpoints and supports optional request/response encryption for sensitive deployments.
